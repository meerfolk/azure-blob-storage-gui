{
  "name": "azure-blob-storage-gui",
  "version": "1.0.0",
  "description": "GUI for simple using azure blob storage",
  "main": "index.js",
  "scripts": {
    "build": "lerna run build && lerna run deploy",
    "start": "lerna run build && lerna run start:app"
  },
  "author": "Meerfolk",
  "license": "ISC",
  "devDependencies": {
    "lerna": "^3.22.1",
    "ncp": "^2.0.0",
    "prettier": "^2.0.5"
  }
}