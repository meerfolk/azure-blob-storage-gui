{
  "name": "azure-blob-storage-gui",
  "version": "1.0.0",
  "description": "GUI for simple using azure blob storage",
  "main": "index.js",
  "scripts": {
    "build": "lerna run build && ncp ./packages/ui/dist ./packages/app/dist/ui && lerna run deploy"
  },
  "author": "Meerfolk",
  "license": "ISC",
  "devDependencies": {
    "lerna": "^3.22.1",
    "ncp": "^2.0.0",
    "prettier": "^2.0.5"
  }
}