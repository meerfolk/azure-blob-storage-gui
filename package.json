{
  "name": "azure-blob-storage-gui",
  "version": "1.0.0",
  "description": "GUI for simple using azure blob storage",
  "main": "index.js",
  "scripts": {
    "copy:ui": "ncp ./packages/ui/dist ./packages/app/dist/ui",
    "build": "lerna run build && npm run copy:ui && lerna run deploy",
    "start": "lerna run build && npm run copy:ui && lerna run start:app"
  },
  "author": "Meerfolk",
  "license": "ISC",
  "devDependencies": {
    "lerna": "^3.22.1",
    "ncp": "^2.0.0",
    "prettier": "^2.0.5"
  }
}